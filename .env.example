# consumed by the postgres image (db service)
POSTGRES_DB=tickets
POSTGRES_USER=tickets
POSTGRES_PASSWORD=tickets

# consumed by the Spring datasource (backend service) - same DB, JDBC form
DATABASE_URL=jdbc:postgresql://db:5432/tickets
DB_USER=tickets
DB_PASSWORD=tickets
